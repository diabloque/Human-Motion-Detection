# Results

This folder stores qualitative outputs and artifacts from trained YOLO runs.

## Included

- `sample_predictions/` - prediction images generated using a quick helmet detection run.

## Notes

- For stronger detections, train with more epochs and/or a larger model.
- Keep large weight files (`*.pt`) out of Git unless using Git LFS.
